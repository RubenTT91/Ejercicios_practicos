/**
 *  + Suma
 * - resta
 * * Multiplicación
 * ** Exponenciación
 * / Division
 * % módulo
 * ++ incrementar
 * -- decrecer
 */

// suma 
let a=3, b=2;
let z = a+b;
let operador="suma";
console.log(`El resultado de la ${operador}, es ${z}`);

//Resta 
let a=3, b=2;
let z = a-b;
let operador="resta";
console.log(`El resultado de la ${operador}, es ${z}`);

//Multiplicación
let a=3, b=2;
let z = a*b;
let operador="multiplicación";
console.log(`El resultado de la ${operador}, es ${z}`);

//División
let a=3, b=2;
let z = a/b;
let operador="división";
console.log(`El resultado de la ${operador}, es ${z}`);

//Residuio %
let a=3, b=2;
let z = a**b;
let operador="exponente";
console.log(`El resultado de la ${operador}, es ${z}`);

//Incremento ++

// PRE-IONCREMENTO
let a=3;
let  z = ++a;
console.log(`a es >> ${}, `);